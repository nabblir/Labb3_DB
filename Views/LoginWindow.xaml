<Window x:Class="Labb3_DB.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Labb3_DB.Views"
        mc:Ignorable="d"
        Title="Fantasy Kingdom Builder" 
        Height="600" 
        Width="800"
        FontFamily="Segoe UI"
        Foreground="AntiqueWhite"
        Background="#34495E"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1">
            
            <TextBlock Text="Login" FontSize="24" FontWeight="Bold" Margin="0,100,0,20" HorizontalAlignment="Center">
                <TextBlock.Effect>
                    <DropShadowEffect/>
                </TextBlock.Effect>
            </TextBlock>

            <TextBox 
                Name ="User" 
                TextWrapping="Wrap" 
                Text="{Binding Username}" 
                Width="120"
                MaxLength="12"
                Margin="0,0,0,10"
                >
                <TextBox.Effect>
                    <DropShadowEffect/>
                </TextBox.Effect>
            </TextBox>

            <PasswordBox 
                    Name="Password" 
                    Width="120" 
                    Margin="0,0,0,20"
                    MaxLength="12"
                    PasswordChar="#"
                    PasswordChanged="PasswordChanged">
                <PasswordBox.Effect>
                    <DropShadowEffect/>
                </PasswordBox.Effect>
            </PasswordBox>

            <Button Content="Login" 
                    Width="100" 
                    HorizontalAlignment="Center" 
                    Margin="0,20,0,20" 
                    BorderBrush="Black"
                    Command="{Binding LoginCommand}"/>
            
            <Button Content="Sign Up" 
                    Width="100" 
                    HorizontalAlignment="Center" 
                    Margin="0,0,0,20" 
                    Background="#FF4B4B88" 
                    BorderBrush="Black"
                    Command="{Binding SignupCommand}"/>
            
            <Button Content="Exit" 
                    Width="100" 
                    HorizontalAlignment="Center" 
                    Margin="0,0,0,20" 
                    Background="#FF964B4B" 
                    BorderBrush="Black"
                    Command="{Binding ExitCommand}"/>
        </StackPanel>
    </Grid>
</Window>
